Write a program to calculate size of binary tree (size=number of nodes in binary tree)

```
int sizeOfBT(struct node* root)
{
  /* Logic to find size */
}
```


**Methods**:
- Recursive approach to find size
- Non-recursive using queue
